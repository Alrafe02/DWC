
const unirArray = resultado => {
    const array = ["array1", "array2", "array3", "array4", "array5"];
resultado = ""
    for(let i = 0 ; i < array.length ; i++) {
        resultado = resultado + array[i] + " ";
    }
    alert(resultado)
    console.log(resultado)
};
